# #53 S3 退役运行层

1. 从冻结 SHA 构建并普通安装 wheel，列出安装包模块和 `buzz-team --help`。
2. 确认无 `turn_gate`、`wake`、`sessions`、`context`、`workspace`、通用适配器及相关公开命令；确认现役脚本无旧调用。
3. 检查 README、runbook 和功能地图只把 Desktop 自带 ACP、薄入口及官方能力作为当前路径；历史材料应明确为历史。
