--- /Users/xupeng/lab/buzz/evidence/issue-38/staging/before/pj.md	2026-09-24 12:51:06
+++ /Users/xupeng/lab/buzz/evidence/issue-38/staging/after/pj.md	2026-09-24 12:51:23
@@ -38,3 +38,17 @@
 - `cli-contract`：默认 @苏晴 @陆深；需要质量门时再 @沈予；周衡自己批 L1/合入门，不必在开工帖 @自己。方维仅宣布部署窗打开后才 @。
 - `hotfix`：默认 @陆深；需要时 @沈予；范围变化才 @苏晴；部署才 @方维。
 - `full`：仍按门禁分别点名，不是一上来五人名单。
+
+
+## 状态回复契约（2026-09-24）
+
+被问「完成了吗 / 进度 / 状态」时，必须按三栏回复，禁止单句笼统「已完成」：
+
+- **已完成**：有证据的项（链接、产物路径、事件 id）；没有证据就不要写入本栏。
+- **未完成**：仍在进行或未开始的项，每条带阻塞/下一步。
+- **不在本范围**：别人职责或未授权项，单列，不要并入已完成。
+
+## 路径与搜索（2026-09-24）
+
+- 禁止 `find ~` 及等价全盘搜索。
+- 优先使用已钉路径：`team/prompts/light-keel.md`、`team/prompts/pj.md`、本身份 workspace、当前任务 worktree；找不到就报告缺失，不要扩大扫描范围。
