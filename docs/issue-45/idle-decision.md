# #38 idle 决策（apply 前）

状态：修订，尚未 apply。禁止把周衡 `BUZZ_ACP_IDLE_TIMEOUT` 从 1500 秒改到 180 秒。

## 测量

- 周衡现值为 1500 秒。其余席位为 180 秒（#38 正文）。
- #38 记录过一次 `find ~` 静默约 1222 秒，触发 long_tool 告警，并靠近 1500 秒的撕池线。
- 合法静默上限取周衡当前 idle：1500 秒。180 秒不是测量结果。

## 决策

- 不 apply 1500→180。
- 绝对 turn 上限与 idle 分开：现网 `BUZZ_ACP_MAX_TURN_DURATION=7200`。它管整轮时长，不管 stdout 静默。
- 覆盖这次上限的受控任务是 sleep 1490（低于 1500，高于已观测的 1222）。完成标准：无 idle 取消，原线程只有一条完成回帖。

## 观测

2026-09-24 19:31 的 `sleep 1490`（event `5633d54b545fc4bd92b629402a2683c2aa634e88696d14e4cd211f46ac190250`）被 harness 在约 15 秒后送去后台。ACP turn `01a0d32f` 于 19:32:53 结束。进程 96355 从 19:32:16 活到 21:41:28 后自己退出。21:42:30 有一条「静默任务完成」。这不满足「turn 内覆盖静默上限」，S3 仍是 fail。更早一次 event `b5ab68e4…` 没有回帖。

## 回滚

本决策不改运行配置。若将来 apply 缩短 idle，先有新的测量，再另作决定。
