# #47 上游会话配置暂定记录（2026-09-26）

## 沈予灰度记录

统计口径：Asia/Shanghai 自然日；按 `sessionId` 去重并取最新 `updatedAt` 的 `usage.json`，按每条已完成 turn 的 `endedAt` 归日。原始汇总在本机私有证据 `~/lab/buzz/evidence/47-native-session-daily.json`，不进入交付包。

| 日期 | 配置 | 已完成 turn | 有完成 turn 的会话 | 输入 token | 输出 token | 总 token | 缺失总量 |
|---|---|---:|---:|---:|---:|---:|---:|
| 2026-09-24 | 灰度前 | 16 | 6 | 10,427,516 | 86,305 | 10,513,821 | 0 |
| 2026-09-25 | 过渡日；约 18:58 才启用 `thread/4` | 11 | 7 | 670,294 | 15,541 | 685,835 | 0 |

9 月 25 日的 11 个 turn 同时包含旧配置和灰度配置，因此尚无一个完整的灰度自然日，S1 不能验收。两天任务量与测试流量也不同；日 token 差额不能归因为会话策略。9 月 25 日未观察到使用量缺失。两个线程隔离及达到四轮后的继续回复见 #47 原灰度证据。2026-09-26 00:00 后重启的沈予 `buzz-acp` 日志仍为 `session_policy=thread`、`max_turns_per_session=4`（本机证据 `~/lab/buzz/evidence/f66ef5e-preview/runtime-8f619a6.json`）。四轮上限是会话轮换条件，不是总 token 预算。

## 九个现役身份的暂定决定

| 身份 | 决定 | 理由与当前状态 |
|---|---|---|
| 沈予 | 暂保留 `thread/4` 灰度配置 | 已核对线程隔离和轮换，尚缺完整灰度自然日用量；运行进程为 `thread/4`。完整日后复核，不将过渡日差额写成节省承诺。 |
| 陆深 | 暂缓推广，保留当前上游默认值 | 开发任务可能跨帖延续，尚无该身份的线程隔离和轮换取证；当前运行进程为 `channel/0`。 |
| 卫平 | 暂缓推广，保留当前上游默认值 | 私聊和频道协作均在用，尚无该身份的线程边界取证；当前为 `channel/0`。 |
| 周衡 | 暂缓推广，保留当前上游默认值 | 项目协调可能引用同频道先前决策，需先确认线程隔离不会丢失所需上下文；当前为 `channel/0`。 |
| 方维 | 暂缓推广，保留当前上游默认值 | 运维任务可能跨帖追踪，需要独立验证恢复路径；当前为 `channel/0`。 |
| 秦牧 | 暂缓推广，保留当前上游默认值 | 尚无该身份的真实线程和轮换取证；当前为 `channel/0`。 |
| 苏晴 | 暂缓推广，保留当前上游默认值 | 产品讨论可能跨帖引用，需先确认线程边界符合工作方式；当前为 `channel/0`。 |
| 裴昭 | 暂缓推广，保留当前上游默认值 | 秘书工作可能跨帖汇总，尚无该身份的上下文取证；当前为 `channel/0`。 |
| 韩川 | 暂缓推广，保留当前上游默认值 | 尚无该身份的真实线程和轮换取证；当前为 `channel/0`。 |

`channel/0` 表示 Desktop 当前未设置这两个环境变量而采用上游默认值。9/9 决定仍为暂定，须在 2026-09-26 完整自然日结束后复核沈予数据再签署 S3。后续需要推广时，应经 Desktop 支持的身份配置入口灰度并核对运行日志；如果缺少受支持入口，标为阻塞并反馈上游。本次没有新增 buzz-team 绑定逻辑，也没有逐身份重绑。

关联：[#47](https://github.com/xforce-io/buzz-team/issues/47)、[#53](https://github.com/xforce-io/buzz-team/issues/53)、[#54](https://github.com/xforce-io/buzz-team/issues/54)。
