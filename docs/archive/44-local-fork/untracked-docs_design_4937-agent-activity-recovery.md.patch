diff --git a/docs/design/4937-agent-activity-recovery.md b/docs/design/4937-agent-activity-recovery.md
new file mode 100644
index 000000000..6dcb0d2e9
--- /dev/null
+++ b/docs/design/4937-agent-activity-recovery.md
@@ -0,0 +1,90 @@
+# 4937：Activity 恢复与长任务结果核对
+
+状态：Approved L1 下的 L2。L1 已于 2026-09-19 获用户确认；本文件覆盖 S2 的实际认证断点，并补充已批准 S1 的交付边界与失败恢复契约；Activity 异常反馈与持久恢复仍须逐项验证，不由认证修复代替。
+
+## 1 背景
+
+关联 [#4937](https://github.com/block/buzz/issues/4937)、[#4223](https://github.com/block/buzz/issues/4223)。封闭 relay 的直接成员通过认证后，有效 NIP-OA 被忽略；所有者未登记，导致活动拒收。采用上游 [PR #5581](https://github.com/block/buzz/pull/5581) 的已提交修复作为待验证输入，不把 PR 自述当验证结论。基线锁定本机 relay 镜像源码 c507a4d488ca27796e78d876b9c24ee38442cc1b，避免混入无关升级。
+
+## 2 名词解释
+
+见 [名词表](../glossary.md)。成员准入与所有者登记是不同事实；直接成员资格不等于不存在所有者。
+
+## 3 目标与非目标
+
+目标：已准入直接成员的有效所有者证明能在 HTTP 事件提交和 WebSocket 认证路径一致登记，真实 Activity 可到达既有 owner 客户端。
+
+非目标：不放宽成员准入；不切换开放 relay；不改变加密协议；不直接修写数据库关系；不迁移数据；不重做 Activity 布局，不引入通用任务调度系统。另关联 #7086：发送预算必须不超过锁定 NIP-44 编解码器的 65,408 字节上限；原 65,535 字节预算会让合法裁剪后的消息仍加密失败。
+
+S1 目标：请求可能已经交付给 ACP 智能体后，超时、传输错误或执行任务崩溃均不得自动重新投递原请求；明确未交付的失败保留已有有界重试。停止重放不代表任务失败或后台进程已终止。
+
+## 4 能力
+
+认证准入成功后解析证明；封闭社区中要求所证明的 owner 同为本社区成员。签名、时间条件、第一位所有者不可替换的约束保持有效。无效证明不产生所有者权限。
+
+交付状态按一次 turn 隔离：首次尝试向子进程写入 `session/prompt` 前进入“可能已交付”，即使写入失败也不回退。取消清理、响应读取和子进程退出不能清除该事实。初始提示同样可能产生副作用，计入本轮交付；正常完成后仅新 turn 建立新状态，旧状态保持可供崩溃恢复判断。
+
+### 4.1 UI/UX
+
+沿用既有 Activity；成功时显示当前真实工具活动。修复前“有执行但无活动”的空状态不能作为完成证据。等待/连接错误反馈属于已批准 L1 的后续子项；本子项沿用既有面板。S1 结果不确定时必须呈现“结果待核对”，保留关联消息、turn、会话与已观察工具，明确自动重试已停止；不能提示用户直接重发而隐去可能发生的执行。用户明确停止或中途补充请求沿用原控制语义，不把普通超时冒充用户的重试授权。
+
+## 5 思路与折衷
+
+分离准入和归属解析，HTTP 与 WebSocket 共用解析函数，保留 Member / ViaOwner 的原准入来源语义。放弃在数据库手工写 owner 和临时移除成员的绕行方案。封闭社区额外查 owner 成员资格以防任意临时身份提升速率类别；查询失败不登记所有者。
+
+S1 放弃依据“没有 tool_call”认定未执行，也不依赖可被清理消耗的 JSON-RPC 请求 ID。一次 turn 的交付事实由执行任务与崩溃恢复共享；执行任务消失不使它回退。明确交付前失败可退避重试；交付后失败保留核对信息并停止自动重放。
+
+## 6 架构
+
+```mermaid
+flowchart LR
+ A[验证认证载体和成员准入] --> B[共享所有者证明解析]
+ B --> C[封闭社区验证 owner 成员资格]
+ C --> D[第一位所有者登记]
+ D --> E[既有活动鉴权与加密转发]
+ B --> F[证明无效：不登记]
+ C --> F
+ D --> G[冲突或存储失败：不授予所有者上下文]
+```
+
+认证、归属解析、持久登记、活动转发保持现有分层。只有已准入调用能进入解析；解析不赋予成员资格。
+
+S1 分层：ACP 写入边界记录交付事实，pool 持有 turn 生命周期，主循环统一决定队列去向，既有 Activity 管道承载可见核对状态。普通结果返回与 JoinSet 崩溃恢复必须使用相同判定。活动发送失败不能被视为未执行；持久核对信息与错误反馈完成前，不宣称 S1 完成。
+
+## 7 模块
+
+relay_members 共享解析；HTTP bridge 与 WebSocket auth 调用该解析。其余 relay 活动处理与客户端消费接口保持现有契约。buzz-core 的活动明文上限对齐现有 nostr 0.44.7 编解码器；ACP 已有裁剪和批处理统一引用此常量，无需另建裁剪实现。
+
+S1 涉及 acp / pool / 主循环队列恢复；不修改 Kairo 业务命令或推断 shell 幂等性。
+
+## 8 API/CLI
+
+N/A：无新增公共请求或参数；已有 NIP-42 / NIP-98 载体与 NIP-OA 时间条件继续生效。
+
+## 9 边界
+
+社区隔离、签名验证、签名载体时间、非成员 owner、缺失证明、冲突 owner、存储失败必须验证。不得用本机 agent-owner 提示替代所有者签名。私钥不进入源码、测试、Issue 或日志。
+
+## 10 迁移/兼容/回滚
+
+无 schema 迁移。真实环境补全既有 owner 对该智能体的证明并通过正式认证登记。只替换 relay 二进制的同基线本机构建；原镜像 sha256:1cf32474cb798ab2dec07b60835424c5895c4edf49b38f24ef052860c447d19f 保留可回滚。更换前备份数据库与运行配置，记录镜像、配置、健康检查及恢复入口。不能删除卷、重建密钥或将测试连至生产数据库。认证关系为合法持久状态，回滚二进制不删除它。
+
+## 11 测试计划
+
+- Unit：签名/时间条件原有用例与新增解析分支；最大活动明文经过真实 NIP-44 加解密往返，超出一字节产生明确预算错误；恢复旧常量必须使边界正例失败。
+- Integration：真实隔离 PostgreSQL/Redis，分别通过生产 HTTP 与 WebSocket 路径验证直接成员有效证明登记、非成员 owner 拒绝、过期/未来证明拒绝；正例证明旧代码会失败。
+- E2E / S2：原本机社区正式认证后读取 owner 登记；触发已授权的只读诊断或观察真实任务，确认 relay 接受真实活动、桌面展示工具、关闭重开仍可读。仅 AUTH OK 或数据库值不能代替 UI 验证。
+- 回归：适用格式、lint、relay 测试；新改动或故障才扩大重复检查。上线前记录通过项与未完成项。
+
+- E2E / S1：模拟 ACP 写入一次业务标记后保持静默，经过真实超时及队列处理后标记仍为 1；结果待核对可见，恢复只能读取既有结果。
+- Integration / S1：覆盖交付前失败、部分写入不确定、已交付但无活动、正常清理后超时、崩溃、连续不同 turn 与明确用户控制。旧自动重试策略必须使正例失败。
+
+## 12 开放问题
+
+本子项完成后仍须落实已批准 L1 的 S1 防重放与 S2 错误/等待反馈，并核验超大活动帧加密失败。原业务任务已独立完成不等于这些项完成。
+
+## 13 关联
+
+- Issue：[4937](https://github.com/block/buzz/issues/4937)、[4223](https://github.com/block/buzz/issues/4223)
+- 参考修复：[5581](https://github.com/block/buzz/pull/5581)，head 2a02440b50dfbd2fe4bf58759c1eff9764e7f488
+- [Activity 愿景](../../VISION_ACTIVITY.md)
